// STARK INDUSTRIES OS - PostCSS Configuration
// CSS Processing Pipeline for Tailwind CSS

export default {
  plugins: {
    // Tailwind CSS - Core styling engine
    tailwindcss: {},
    
    // Autoprefixer - Cross-browser compatibility
    // Karena kita tidak punya waktu untuk menulis vendor prefixes manual
    autoprefixer: {},
  },
}